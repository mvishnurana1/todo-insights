enum frequency {
    oneDay = 1,
    week = 7,
    month = 30,
    year = 365,
}

const monthSize = 30;
const weekSize = 7;
const yearSize = 365;

function getRemaining(currentSize: number) : number[] {
    currentSize = 4;

    return [1, 3, 4];
}
